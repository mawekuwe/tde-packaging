Note on 2013/07/17 :

The current TDE packages are built as follow:

For 3.5.13.x:
1) build "dependencies/qt3" from "mageia" directory
2) build all other packages from "redhat" directory

For R14 and later:
1) build all packages from "redhat" directory
